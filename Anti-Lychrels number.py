"""
An Anti-lychrels number is a number that forms a palindome thourg thr iterative process ....
"""

#since i ain't trying to solve for all primes am good

#first ill itroduce the ignition
n = 0
start = "True" # oh god i hate the while statement
def Primer(self, value):
    while start == "True":
        if value%n == 0:
            #ie the loops goes on properly
            break
        else:
            #we have a prime
            value.inverter()
#An idea is to split the value into its constituents
# Another one is to 
def inverter(self, value):
    pre_raw = [value]
    raw = split.pre_raw
    #substitute last string and first string
    post_raw = 

def 
